yfinance>=0.2.40
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
plotly>=5.20
pyarrow>=14.0
vaderSentiment>=3.3.2
feedparser>=6.0
langgraph>=0.2
langchain-anthropic>=0.2
# LSTM strategy requires TensorFlow — install separately, not included here
# (too large for cloud free tier; app runs fine without it, LSTM tab is disabled)
# Local install: pip install tensorflow-macos tensorflow-metal  (Apple Silicon)
#                pip install tensorflow                          (Linux / Intel)
