yfinance>=0.2.40
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3
streamlit>=1.35
plotly>=5.20
pyarrow>=14.0
# Deep learning for LSTM strategy
# Apple Silicon (M1/M2/M3) — already installed in this env:
#   pip install tensorflow-macos tensorflow-metal
# Intel Mac / Linux / Windows:
#   pip install tensorflow>=2.13
tensorflow-macos>=2.13; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow-metal>=1.0; sys_platform == "darwin" and platform_machine == "arm64"
